# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Test and Build

on:
  push:
    branches: [ "main" ]

env:
  AWS_ROLE_NAME: ${{ vars.MY_VAR }}

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: fastapi
          POSTGRES_PASSWORD: fastapi
          POSTGRES_DB: fastapi_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U fastapi"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v5
    - name: Access Environment Variables
      run: |
        echo "AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}"
        echo "AWS_REGION: ${{ vars.MY_VAR }}"
        echo "AWS_ROLE_NAME: ${{ env.AWS_ROLE_NAME }}"
    - name: Set up Python 3.14
      uses: actions/setup-python@v3
      with:
        python-version: "3.14"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install "psycopg[binary]"
    - name: Lint with ruff
      run: |
        ruff check .
    - name: Run migrations
      env:
        DATABASE_URL: postgresql+psycopg://fastapi:fastapi@localhost:5432/fastapi_test
      run: |
        alembic upgrade head
    - name: Test with pytest
      env:
        DATABASE_URL: postgresql+psycopg://fastapi:fastapi@localhost:5432/fastapi_test
      run: |
        pytest tests
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build and push Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: Dockerfile
        push: true
        tags: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:latest
        labels: |
          org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ github.event.repository.name }}
          org.opencontainers.image.description=Docker image for ${{ github.event.repository.name }}
          org.opencontainers.image.licenses=MIT
          org.opencontainers.image.authors=${{ github.actor }}
          org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ github.event.repository.name }}
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}
    - name: Retrieve Secrets from AWS Secrets Manager
      uses: aws-actions/aws-secretsmanager-get-secrets@v2
      with:
        secret-ids: production/secret # Retrieves the entire secret JSON
        parse-json-secrets: true # Parses JSON secrets into individual environment variables
